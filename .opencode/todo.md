# Mission Tasks — [TARGET_DOMAIN]

## This Session
[ ] Fill in all [PLACEHOLDER] values in AGENTS.md, scope.md, findings.md
[ ] Run passive recon — use `recon` command in OpenCode
[ ] Analyze response headers — use `headers` command
[ ] Map all routes manually or via probe_template.py

## Pending Verification
[ ] Mass assignment — does extra field store in DB?
[ ] Bearer token endpoint — try /api/login, /api/auth/login, /oauth/token
[ ] SSTI — did {{7*7}} render as 49 on any page?
[ ] Rate limiting — test XFF bypass with `throttle` command

## Completed
[x] Initial recon
